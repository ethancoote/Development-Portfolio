---
const {text, href} = Astro.props;
---

<a href={href} class="flex group shrink-0 gap-2 hover:text-neutral-100 transition-[0.3s] hover:cursor-pointer select-none items-center">
    <p>{text}</p>
</a>